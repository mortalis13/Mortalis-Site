<template>
<div>
<p class="mb7">As all <b>Sublime Text</b> plugins, this one is written in <b>Python</b>.</p>
<p class="project-features">Contribution to the project</p>
<ul>
  <li>added <b>'adb_app_package'</b> option for filtering by the app package (full or partial) rather than by tag name or raw text (the option searches the app <b>PID</b> and shows all messages for that PID)</li>
  <li>unified filtering regex patterns for better control over the log columns grouping</li>
  <li>refactored some parts of the code</li>
</ul>
</div>
</template>